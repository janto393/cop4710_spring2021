// React imports

import React from "react";

type loginPageType = {};

const LoginPage: React.FC = (props: loginPageType) => {
  return (
    <div>
      <h1>Hello, World!</h1>
    </div>
  );
};

export default LoginPage;
